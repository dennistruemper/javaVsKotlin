package com.example.demo.examples

//easy way to build a string in a readable way
fun main(args: Array<String>) {
    val s = "abc"
    println("$s.length is ${s.length}") // prints "abc.length is 3"
}
